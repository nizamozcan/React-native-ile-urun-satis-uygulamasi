import React, {useEffect, useState} from 'react';
import {SafeAreaView, StyleSheet, Text, View, FlatList} from 'react-native';
import MusicApp from './Src/MusicAppuygulaması/MusicApp';

const App = () => {
  return (
    <SafeAreaView>
      <MusicApp />
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({});

export default App;
